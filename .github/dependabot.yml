# No pip entry: scan.py imports only the standard library, so there is no
# manifest to update and the pip ecosystem fails on every run.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
